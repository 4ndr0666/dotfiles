if [[ ! $GRUB_CMDLINE_LINUX_DEFAULT =~ ibt=[^[:space:]]+ ]]; then
    GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT:+$GRUB_CMDLINE_LINUX_DEFAULT }ibt=off"
fi
if [ -z "${GRUB_DISABLE_OS_PROBER+x}" ]; then
  GRUB_DISABLE_OS_PROBER=false
fi

if [[ ! $GRUB_CMDLINE_LINUX_DEFAULT =~ ipv6.disabled=[^[:space:]]+ ]]; then
    GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT:+$GRUB_CMDLINE_LINUX_DEFAULT }ipv6.disabled=1"
fi
if [ -z "${GRUB_DISABLE_OS_PROBER+x}" ]; then
  GRUB_DISABLE_OS_PROBER=false
fi

if [[ ! $GRUB_CMDLINE_LINUX_DEFAULT =~ systemd.unified_cgroup_hiearchy=[^[:space:]]+ ]]; then
    GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT:+$GRUB_CMDLINE_LINUX_DEFAULT }systemd.unified_cgroup_hiearchy=1"
fi
if [ -z "${GRUB_DISABLE_OS_PROBER+x}" ]; then
  GRUB_DISABLE_OS_PROBER=false
fi

if [[ ! $GRUB_CMDLINE_LINUX_DEFAULT =~ ipv6.autoconf=[^[:space:]]+ ]]; then
    GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT:+$GRUB_CMDLINE_LINUX_DEFAULT }ipv6.autoconf=0"
fi
if [ -z "${GRUB_DISABLE_OS_PROBER+x}" ]; then
  GRUB_DISABLE_OS_PROBER=false
fi

if [[ ! $GRUB_CMDLINE_LINUX_DEFAULT =~ accept_ra=[^[:space:]]+ ]]; then
    GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT:+$GRUB_CMDLINE_LINUX_DEFAULT }accept_ra=0"
fi
if [ -z "${GRUB_DISABLE_OS_PROBER+x}" ]; then
  GRUB_DISABLE_OS_PROBER=false
fi

